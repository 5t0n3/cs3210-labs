- Console directly uses MiniUart, which in turn relies on the GPIO and timer drivers
  - The drivers also rely on the Volatile class of pointers
- StackVecs are used to buffer user input & hold command arguments